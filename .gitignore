node_modules/

.next/

# This project uses yarn only — never commit an npm lockfile
package-lock.json

# Environment files
local.env
.env.local
.env.local.backup
.env*.local
.env*
.vercel

# Playwright artifacts
test-results/
playwright-report/

# Local scratch data (label-studio db, media, exports) — not source
mydata/
